<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nvd.mycookingshare">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_MEDIA_STORAGE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyCookingShare">
        <activity android:name=".MyRecipeActivity" android:label="Công thức của tôi"></activity>
        <activity
            android:name=".PersonalActivity"
            android:label="Cá nhân"></activity>
        <activity
            android:name=".LoginActivity"
            android:label="Đăng nhập">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainTabActivity" />
        <activity
            android:name=".FavoriteRecipeActivity"
            android:label="Công thức yêu thích" />
        <activity
            android:name=".ViewRecipeActivity"
            android:label="Công thức" />
        <activity
            android:name=".AllRecipesActivity"
            android:label="Trang chủ" />
        <activity
            android:name=".MainActivity"
            android:label="Thêm công thức" />
    </application>

</manifest>